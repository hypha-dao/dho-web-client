export const isConnected = ({ connected }) => connected
export const profile = ({ view }) => view
export const profiles = ({ profiles }) => profiles
export const loadings = ({ loadings }) => loadings
