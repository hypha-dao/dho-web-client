query membersPagination ($daoId: String!, $first: Int, $offset: Int, $filter: MemberFilter) {
  getDao (docId: $daoId) {
    docId,
    member (first: $first, offset: $offset, filter: $filter) {
      docId
      __typename
      ... on Member {
        details_member_n
        createdDate
      }
    }
  }
}
